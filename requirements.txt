# ============================================================
# requirements.txt
# ALKF Master Land Plan API  v1.0
#
# Versions are unpinned to allow pip to resolve compatible
# wheels for the Render runtime (currently Python 3.14).
# setuptools is listed explicitly to fix pkg_resources errors
# that occur on newer Python versions.
# ============================================================

# Build system fix (required for Python 3.12+)
setuptools

# API framework
fastapi
uvicorn[standard]

# Geospatial core
geopandas
shapely
pyproj
fiona

# OSM data
osmnx

# Numerical
numpy
scipy

# HTTP (resolver API calls)
requests

# DXF export
ezdxf

# Lease plan parsing
opencv-python-headless
pdf2image

# Noise pipeline internals (reused from view.py / noise.py)
pandas
networkx
